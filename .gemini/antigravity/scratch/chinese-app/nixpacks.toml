[phases.setup]
nixPkgs = ["python311", "pip"]
aptPkgs = ["libxcb1", "libsm6", "libxext6", "libxrender-dev", "libgomp1"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn ocr_server:app --host 0.0.0.0 --port $PORT"
